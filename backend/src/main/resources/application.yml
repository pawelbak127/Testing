server:
  port: 8080

spring:
  application:
    name: TestManagement
  datasource:
    url: jdbc:postgresql://localhost:5432/test_management
    username: expert
    password: expert
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true